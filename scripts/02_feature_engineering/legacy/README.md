# 特征工程原始模块

这里保存毕业设计研究阶段的原始模块，便于追溯算法演化。当前代码尚不能作为独立、可复现的软件包直接运行。

主要阻塞包括：硬编码本地路径、缺少外部模型与停用词资源、Notebook 与模块版本不完全一致、`process_data()` 缺少返回值，以及正式 19 列与 5 个候选列尚未在代码层分离。

正式重构时将把预处理、文本指标、视觉指标、特征注册表和配置拆成独立模块，并以测试固定每个指标的输入、公式、单位、空窗口处理和输出列名。
